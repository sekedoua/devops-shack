apiVersion: v1
kind: Service

metadata:
  name: chatbot-service
  labels:
    app: chatbot

spec:
  type: LoadBalancer

  selector:
    app: chatbot

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
